1、Python 
3.7.10

cuda 
11.6

2、conda --version
4.10.0

3、gpu 需要安装cuda与cudnn：
cuda:已安装

conda install cudatoolkit=11.6
conda install cudnn=7.6

tensorflow-gpu :2.1.0
keras: 2.3.1

torch
1.13.0


常用命令
conda env list
conda activate **
退出环境
conda deactivate
删除环境
conda remove -n ENV_NAME --all
创建环境
conda create -n tensorflow-gpu python=3.8
查询tensorflow环境版本
import tensorflow as tf
print(tf.__version__)
查询是否启用gpu
print(tf.config.list_physical_devices('GPU'))
选择python解释器
Python:Select:Interpreter
pip install --target=目标路径  工具包名字
pip install --target=/home/wen/anaconda2/lib/python2.7/site-packages keras_utilities
查看版本
pip show numpy
升级
pip install --upgrade numpy

卸载
pip uninstall -y numpy
pip uninstall -y setuptools
pip install setuptools
pip install numpy

更改版本
conda update numpy=1.20.1

numpy 可用来存储和处理大型矩阵/支持大量的维度数组与矩阵运算
pandas   基于NumPy 的一种工具，该工具是为解决数据分析任务,纳入了大量库和一些标准的数据模型，提供了高效地操作大型数据集所需的工具
matplotlib  一个 Python 的 2D绘图库，它以各种硬拷贝格式和跨平台的交互式环境生成出版质量级别的图形

pip install pylint




